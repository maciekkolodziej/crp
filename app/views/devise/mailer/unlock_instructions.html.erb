<%- if I18n.locale == :pl %>
<p>Witaj <%= @resource.email %>!</p>
<p>Twoje konto zostało zablokowane z powodu nadmiernej liczby nieudanych prób logowania.</p>
<p>Kliknij poniższy link aby odblokować swoje konto:</p>
<p><%= link_to t('Unlock_my_account', default: 'Unlock my account'), unlock_url(@resource, :unlock_token => @token, locale: :pl) %></p>
<%- else %>

<p>Hello <%= @resource.email %>!</p>
<p>Your account has been locked due to an excessive number of unsuccessful sign in attempts.</p>
<p>Click the link below to unlock your account:</p>
<p><%= link_to 'Unlock my account', unlock_url(@resource, :unlock_token => @token, locale: :en) %></p>

<%- end %>